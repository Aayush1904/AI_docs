{
  "buildCommand": "npm run build",
  "installCommand": "rm -rf node_modules package-lock.json && npm install && npx prisma generate",
  "functions": {
    "app/**/*.js": {
      "runtime": "nodejs18.x"
    }
  }
}
